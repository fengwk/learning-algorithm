package fun.fengwk.learning.algorithm.graph.directed;

/**
 * 强联通分量
 *
 * @author fengwk
 */
public class SCC {

    public SCC(DirectedAdjSet G) {
        /*
        DFS dfs = new DFS(G.reverse());
        List<Integer> postorder = dfs.postorder();
        Collections.reverse(postorder);

        for (int v : postorder) {
            if (visited[v] == -1) {
                dfs(v);
                sccount++;
            }
        }
        */
    }

}
